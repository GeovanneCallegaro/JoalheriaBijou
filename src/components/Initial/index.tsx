import { SectionInformation } from '../SectionInformation'
import { Slider } from '../Slider'
import './styles.css'

export const Initial = () => {
    return (
        <div>
            <Slider />
            <SectionInformation />
        </div>
    )
}